const ecModel = require('../models/EnergyCommunity.js');
const db = require('../model/config.js');

const ecController = () => {
    const add = (req,res)=>{
        
    }
}


module.exports = ecController;