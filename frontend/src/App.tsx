import { useReducer, useState } from 'react';
import './App.css';

function App() {
  const [count, setCount] = useState(0);
  const form = useReducer((state, action)=>{
    
  },[])

  return (
    <>
      <div>

      </div>
    </>
  )
}

export default App
